# Web dashboard

**Stack:** React + Vite — [ADR-0002](../../docs/adr/0002-stack-pin.md).

This is the **primary product UI**: project list, git connect, init, profiles,
configurable widgets (gate / bugs / branch / runs), settings.

Talks to `apps/api` over HTTP.
