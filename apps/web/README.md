# Web dashboard

**Stack:** React 19 + Vite 6 — [ADR-0002](../../docs/adr/0002-stack-pin.md).

```bash
pnpm --filter @agent-kernel/web dev   # http://127.0.0.1:5173 (proxies /api → :8787)
```

Primary UI: project list + register form. Init / Lawpack plant comes next.
