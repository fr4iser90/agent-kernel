================================================================
RESUME — {{PRODUCT_NAME}} (thin pointer)
================================================================

Obey **agent-kernel** `roles/followup.md` fully
(fix → validate every 3 features → feature).

**ACCEPT / fantasy** only from Initial:
  {{path-to-initial-or-PROGRESS-summary}}

**RUN_ID (fixed):** `agent/{{slug}}-{{YYYYMMDD}}`
- One experiment = **this branch only**.
- **FORBIDDEN:** new `agent/{{slug}}-*` slugs (`-v2`, `-p*`, `-rebased`).
- If tip missing: `git fetch && git checkout -B agent/{{slug}}-{{YYYYMMDD}} origin/main`
  — same name, never a new id.
- PROGRESS.md NOW must list this exact branch.

Do not paste this instead of followup.md unless the harness needs a per-product
path — then still execute kernel Followup as the cycle.
